import { Booking } from "@prisma/client";

interface BookingListProps {
    booking: Booking
}
const BookingLista = ({booking} : BookingListProps ) => {
    return ( <></> );
}
 
export default BookingLista;